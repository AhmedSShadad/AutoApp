- uses: dev-drprasad/delete-older-releases@v1.1.0
  with:
    repo: <AhmedSShadad>/<AutoApp> # defaults to current repo
    keep_latest: 3
    delete_tag_pattern: beta # defaults to ""
  env:
    GITHUB_TOKEN: ${{ secrets.TOKEN }}
